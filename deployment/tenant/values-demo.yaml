# Demo environment (ADR-028): the Hetzner k3s cluster built by roots/hcloud, deployed with
# TENANT values into the namespace-scoped Role (proves the tenant contract on the own-cluster
# path). Layer on top of values-tenant.yaml: `make deploy-tenant ENV=demo`.
# Residency is declared honestly: DE for the cluster and both stores (ADR-028 §4, ADR-036 §6).
residency: DE
image:
  # The package ghcr.io/<owner>/energy-platform is private (the repository is): the author creates
  # this docker-registry Secret in the namespace from a classic PAT with read:packages only
  # (`make print-demo-secret-template`; plan P5-D21).
  pullSecrets: [ghcr-pull]
postgres:
  mode: statefulset
  storageClassName: local-path          # k3s default provisioner, backed by the Postgres volume
  # local-path creates the volume on whichever node the pod lands on, and only the server has the
  # 10 GB Hetzner volume mounted under the provisioner root (roots/hcloud): pin Postgres there.
  # 2026-09-23: without it the first deploy put Postgres on the agent's root disk.
  nodeSelector:
    kubernetes.io/hostname: energy-platform-demo-server
  size: 8Gi
  walArchive:
    size: 2Gi
  backup:
    enabled: true
    baseSchedule: "15 2 * * *"          # ADR-036 amendment 1: one base a day
    walSchedule: "*/10 * * * *"         # RPO 15 min with archive_timeout 300
    walStaleAfterSeconds: 1800
    baseStaleAfterSeconds: 172800
bronze:
  endpoint: https://nbg1.your-objectstorage.com        # store A (V-12)
  bucket: energy-platform-bronze                       # roots/hcloud output bucket_a
  region: nbg1
  retention:
    mode: COMPLIANCE                                   # the store-A control (V-12)
    days: 90
  tiering:
    mode: none                                         # never lifecycle on this gateway (V-12)
  replica:
    enabled: true
    # OCI S3-compatible endpoint https://<namespace>.compat.objectstorage.eu-frankfurt-1.oraclecloud.com.
    # The namespace (`oci os ns get`) is a tenancy identifier the repository does not carry:
    # `make deploy-tenant ENV=demo` builds the endpoint from DEMO_OCI_NAMESPACE (a GitHub
    # repository variable in the deploy workflow). Left empty here, the render fails loudly.
    endpoint: ""
    bucket: energy-platform-bronze-replica             # roots/hcloud output bucket_b (retention rule, V-13)
    region: eu-frankfurt-1
    # ADR-036 amendment 2: the independent copy follows the 15-minute capture cadence, seven
    # minutes after each capture instant (the `check --min-age 5m` then covers that capture);
    # RPO for a store-A loss = 15 min + copy time
    schedule: "7,22,37,52 * * * *"
    staleAfterSeconds: 2700
secrets:
  existingSecret: energy-platform                      # created by the author from the Terraform outputs
metrics:
  enabled: true
  operator:
    enabled: false
grafana:
  enabled: true                       # ADR-039: private dashboards; the Secret carries GRAFANA_ADMIN_PASSWORD / GRAFANA_DB_PASSWORD (added 2026-09-24)
alerting:
  enabled: true                       # ADR-040 (review 3 R3): the rules evaluated on the demo, delivered to the platform receiver
  kubeStateMetrics:
    rbac:
      create: false                     # the OIDC deploy identity may not manage Roles (ADR-035 amendment 1): the author
                                        # applied deployment/tenant/demo-kube-state-metrics-rbac.yaml once with the admin kubeconfig
    apiServer:
      # the `kubernetes` Service IP (k3s service-cidr 10.43.0.0/16) and the k3s server on the private
      # network = `kubectl get endpoints kubernetes` → 10.10.1.10:6443 (read 2026-09-25)
      cidrs: [10.43.0.1/32, 10.10.1.10/32]
      ports: [443, 6443]
  alertmanager:
    # ADR-040 amendment 1 (Phase 14): the channel's credential lives in this Secret, which the author
    # created in the namespace (Level 3, 2026-09-25 22:40 UTC) — the mount is optional, so the release
    # ran before it existed:
    #   kubectl -n energy-platform create secret generic energy-platform-alertmanager --from-literal=smtp-password='…'
    # The receiver, the routes and the egress netblocks below follow ci/receiver-values.yaml
    # (docs/07 §7.3); the password is never a value (05 C-76).
    existingSecret: energy-platform-alertmanager
    egress:
      # Gmail's authenticated submission service, smtp.gmail.com:587 (Hetzner Cloud blocks outbound
      # 25 by default). Google's SPF record is NOT the submission service: it lists two outbound
      # sending ranges and smtp.gmail.com resolves outside them (142.251.127.108, 2026-09-26).
      # The published source that covers the submission hosts is Google's complete range list —
      # every IPv4 prefix of https://www.gstatic.com/ipranges/goog.json (creationTime
      # 2026-09-25T13:07:32, 130 prefixes; the pod network is IPv4):
      #   curl -s https://www.gstatic.com/ipranges/goog.json \
      #     | python3 -c 'import json,sys; [print(p["ipv4Prefix"]) for p in json.load(sys.stdin)["prefixes"] if "ipv4Prefix" in p]'
      # A renumbering shows up as a dial timeout in Alertmanager's notifier log, never as silent loss.
      cidrs:
        - 8.8.4.0/24
        - 8.8.8.0/24
        - 8.34.208.0/20
        - 8.35.192.0/20
        - 8.228.0.0/14
        - 8.232.0.0/14
        - 8.236.0.0/15
        - 23.236.48.0/20
        - 23.251.128.0/19
        - 34.0.0.0/15
        - 34.2.0.0/16
        - 34.3.0.0/23
        - 34.3.3.0/24
        - 34.3.4.0/24
        - 34.3.8.0/21
        - 34.3.16.0/20
        - 34.3.32.0/19
        - 34.3.64.0/18
        - 34.4.0.0/14
        - 34.8.0.0/13
        - 34.16.0.0/12
        - 34.32.0.0/11
        - 34.64.0.0/10
        - 34.128.0.0/10
        - 35.184.0.0/13
        - 35.192.0.0/14
        - 35.196.0.0/15
        - 35.198.0.0/16
        - 35.199.0.0/17
        - 35.199.128.0/18
        - 35.200.0.0/13
        - 35.208.0.0/12
        - 35.224.0.0/12
        - 35.240.0.0/13
        - 35.252.0.0/14
        - 64.15.112.0/20
        - 64.233.160.0/19
        - 66.102.0.0/20
        - 66.249.64.0/19
        - 70.32.128.0/19
        - 72.14.192.0/18
        - 74.114.24.0/21
        - 74.125.0.0/16
        - 104.154.0.0/15
        - 104.196.0.0/14
        - 104.237.160.0/19
        - 107.167.160.0/19
        - 107.178.192.0/18
        - 108.59.80.0/20
        - 108.170.192.0/18
        - 108.177.0.0/17
        - 130.211.0.0/16
        - 136.22.2.0/23
        - 136.22.4.0/23
        - 136.22.8.0/22
        - 136.22.160.0/20
        - 136.22.176.0/21
        - 136.22.184.0/23
        - 136.22.186.0/24
        - 136.23.39.0/24
        - 136.23.48.0/20
        - 136.23.64.0/18
        - 136.64.0.0/11
        - 136.107.0.0/16
        - 136.108.0.0/14
        - 136.112.0.0/13
        - 136.120.0.0/22
        - 136.121.8.0/21
        - 136.124.0.0/15
        - 142.250.0.0/15
        - 146.148.0.0/17
        - 152.238.0.0/16
        - 152.239.128.0/17
        - 162.120.128.0/17
        - 162.216.148.0/22
        - 162.222.176.0/21
        - 172.110.32.0/21
        - 172.217.0.0/16
        - 172.253.0.0/16
        - 173.194.0.0/16
        - 173.255.112.0/20
        - 177.176.0.0/16
        - 177.178.0.0/15
        - 177.208.0.0/15
        - 179.67.0.0/17
        - 179.69.128.0/17
        - 179.193.128.0/17
        - 179.199.0.0/17
        - 186.242.0.0/17
        - 186.245.0.0/16
        - 187.78.0.0/17
        - 187.79.0.0/17
        - 187.126.128.0/17
        - 189.24.128.0/17
        - 189.48.0.0/16
        - 189.49.128.0/17
        - 189.70.0.0/15
        - 189.82.0.0/15
        - 189.105.128.0/17
        - 189.106.0.0/15
        - 191.0.128.0/17
        - 191.2.0.0/15
        - 191.40.128.0/17
        - 191.44.128.0/17
        - 191.45.128.0/17
        - 191.46.0.0/15
        - 191.212.0.0/15
        - 191.216.128.0/17
        - 191.218.0.0/17
        - 191.220.0.0/15
        - 192.104.160.0/23
        - 192.158.28.0/22
        - 192.178.0.0/15
        - 193.186.4.0/24
        - 199.36.154.0/23
        - 199.36.156.0/24
        - 199.192.112.0/22
        - 199.223.232.0/21
        - 200.226.0.0/16
        - 207.175.0.0/16
        - 207.223.160.0/20
        - 208.65.152.0/22
        - 208.68.108.0/22
        - 208.81.188.0/22
        - 208.117.224.0/19
        - 209.85.128.0/17
        - 216.58.192.0/19
        - 216.73.80.0/20
        - 216.239.32.0/19
        - 216.252.220.0/22
      ports: [587]
    receivers:
      - name: ops-email
        email_configs:
          - to: jalalhussein@gmail.com
            from: jalalhussein@gmail.com
            smarthost: smtp.gmail.com:587
            auth_username: jalalhussein@gmail.com
            auth_password_file: /etc/alertmanager/secrets/smtp-password   # the Secret's key, read at send time
            require_tls: true
            send_resolved: true
    routes:
      # 1. the two freshness alerts whose publication expectation is not calibrated yet (a day-ahead
      #    target's lateness is detected up to a day late; the daily settlement's publication timing
      #    is unverified — Phase 15): logged by the platform receiver, not paged
      - receiver: platform-sink
        matchers:
          - alertname = EnergyPlatformTargetLate
          - target =~ "ote_dam|ote_imbalance_settlement"
      # 2. everything that pages goes to the operator's mailbox …
      - receiver: ops-email
        matchers: ["severity = page"]
        continue: true
      # 3. … and every alert, whatever its severity, stays in the receiver's log — the delivery record
      - receiver: platform-sink
egress:
  # k3s kube-router applies a new pod's policy asynchronously (measured 2026-09-23: the
  # metadata service answered a fresh pod, refused it 15 s later): gate every platform pod (05 C-65)
  policyGate:
    enabled: true
  clusterCIDRs: [10.42.0.0/16, 10.43.0.0/16]           # k3s cluster-cidr / service-cidr (cloud-init)
  objectStore:
    # Object-store endpoints are public addresses: capture pods reach them through the 443 rule
    # already (public ranges); process/gaps/hook/drill/backup/replicate pods need this explicit
    # rule (plan P5-D22, provider facts as of 2026-09-23):
    # - store A: nbg1.your-objectstorage.com resolves to 88.198.120.0, RIPE netblock
    #   88.198.120.0/25 (maintained by Hetzner, mnt-by HOS-GUN);
    # - store B: Oracle's public_ip_ranges.json, tag OBJECT_STORAGE, region eu-frankfurt-1
    #   (https://docs.oracle.com/en-us/iaas/tools/public_ip_ranges.json).
    # A provider renumbering shows up as failing process/backup Jobs, never as silent loss.
    cidrs: [88.198.120.0/25, 134.70.40.0/21, 134.70.48.0/22]
    ports: [443]
smoke:
  manifest: ote_intraday_market/manifest.yaml
  fixture: ote_intraday_market/fixtures/ordinary_day
drills:
  restore:
    enabled: true
    schedule: "30 3 * * *"
