"""Placeholder so `make check` has a test to run in Phase 0. Replaced in Phase 1."""

import energy_platform


def test_package_imports_and_has_version() -> None:
    assert energy_platform.__version__ == "0.0.1"
